name: smart_irrigation
description: A smart irrigation system with DRY/WET control.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  firebase_core: ^2.15.1
  firebase_database: ^10.2.5
  fl_chart: ^0.63.0
  syncfusion_flutter_gauges: ^23.1.36
  provider: ^6.0.5
  intl: ^0.18.1
  google_fonts: ^5.1.0
  lottie: ^2.6.0
  shared_preferences: ^2.2.1
  flutter_svg: ^2.0.7
  shimmer: ^3.0.0
  timeago: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/